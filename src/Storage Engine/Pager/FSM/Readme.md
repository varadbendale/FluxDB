The Pager 
This piece of code is merely responsible for getting the data which we have in the page the page is a data structure made for storing all the data blob which have 
before anything lets just go and look what exactly this sturcture carries stuff so first the strucutre is like the slots , header  and the normal data  the slots have three things offet , size and primary key number now these kind of come in clutch when we like need to remove some of the data and then replace it with something put the data and just update the slot easy as that comming to the headder we have the page_num unique for each pagetpye i intended to keep it for like is the page used for normal purpose or hashing or toast table current offset and current size well these are just for keeping the track of thre most recent insertion from like the very back so yeah prety much used for the temporary stuff then the free size entire page have how much free size and the normal_free_size just from back from the last data to the end how much size it is it reepresnts that and the dead_slots these are the bianry numbers for each and everhy slot in the page which like looks for if the slot is dead or not have used bit manipulation it its extend so yeah 

I am heavily inspired by the postgres sql storage engine for this version like the very first thing comming is the fsm it is liek wherever there is space to insert data we insert it and manage all the space and put the data in it basic idea 
get_page_from_file used to get the page from the file suppose we have a page in which already something is present we need that one thing and this function helps us to get it and same is the get_pagezero purpose just read the header from the file and then use it 

find_the_size_of_stuff_get_the_data_and_get_the_primary_key_num the bigger the name the bigger its purpose is see we just have the data comming from the insert parser but we do need to get the size of the entire data and then also the data is stored in the format of the blob the data type and then the data and if the string is there the size as well , now we do need the priamry keys as well if we need to sort it which we ofc do so we get it from here as well the header is passed the data we got is passed and then all these three essential stuff is gotten 

now comming on the main function if there is enoguh space for the data to be in the page and also there si enough space to accomadate the data from the back like no need to reshuffle anything we just put the data directly in it with the updation of the slots and if the page have enough size but not the space from behind we first check on all the dead slots which are there from the bit manipulation from the struct wehich i talked abotu earlier each and every of the data is picked checked if it can accomadate the data and then we move forward if we found well and good just put in it and update the sizes as there can be some sspace left in the earlier slot was bigger and the new one si smaller so that way and even if that thing dont work we need to rearrange everything all the small small spaces which are there all the small dead slots which are of no use we need to reshuffule all and then get the thing so we pretty much like keep the pointer if the slot is not dead we keep the pointer to the tail of it and move forward even if the new slots are dead or some space was left we can just get it at the end so that we can end these internal fragmentations so pretty much we do the memmove tool and shift all the data that way and after that we just add the data which we had in the page simple as that 