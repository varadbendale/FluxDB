1. Having function calling is completly broken need to check about the aggregate functions which are there and when to
   take the agg_final and not 

2. The flow of the table picking is still quite incomplete please have a look at it like for example if there is no 
    table name is given we just do it directly cuase its just directly the first one but if the table name is given 
    we pretty much need to get that table name and check it out so yeah 

3. same is the issue for the stuff we define it with as we pretty much need to add the tables or the columns in the list 

4. In the bytecode the struct which are made the calling of the attrivutes are a hell lot of wrong need to fix it out
    . needs to be replaced with the -> the origianl one is correct the newer ones aint that correct . 

5. In the parser in the comp_2 the on is not being accountable like the on needs to be a seperate node compilation needs it 

6. In the parser the issue is in the join the loops goes till we dont reach to the where or anything else like that we pretty 
   much need to like add the breaking point which is , as well and as few of the edgecases were added for the , so it pretty much 
   still work that way so yeah need to do , not that hard just need to like remove the , handling ( not the braces one ) 

7. In the parser the comp_2 the problem is na the as clause is not getting correct for the normal ones like for the normal tables 
   its not working or written code itself need to write it for normal thing as well ( its everywhere ) 
   (update) i fixed the comp_2 issue in here but the rest everywhere the issue persists 

8. The issue is not the issue just a small optimisation  just in the when i am do the expre or any of the varient the while
   passing the struct first time the same of the struct is been passed where the functions is giving the value again so 
   kind of not that cool 

9. Where clause optimised engine is remaining and also the orderby and the groupby the plain sorting techniques is kind of
   pissing me off so yeah maybe something like that is needed as well 

10. About the lookup_table it requires the name of the table for searching but here if you see in the open_read_op we are giving 
    the page number of the thing so its quite broken need to fix in like the join and the in the compile_select there i am right 
    now putting some random terms but yeah do fix them in future 

11. The sql master is been taken as the temporary solution for now so yeah once the entire thing is done the storage engine 
    the execution engine needs to be updated as per it is made 

12 . The hash for equal join clause dont power the normal stuff which are not the columns so need to get them supported 

13. See wherever the sorting of the data is dont let it be in the groupby orderby or anywhere the checking that if the btree
    is present or not is important cuase if the btree is aalready made there aint any need to make stuff again 

14. the register counter is mess for the ones which i dont want to be displayed so need to fix that up 

15. The issue is there in the join right now while building i just took there is onyl one join in accountibilty so the data 
    structure where the hash and the ranges are stored are just taken one so pretty much need to give each of the join a number 
    and then like store the details of  the stuff as per the counter and put it in the array like that 

16. Not a problem but a future scope in the tree two of the just the numeric terms ( the ones which are not columns like just 3+ 2 )
    are not quite supported yeah a singular operation is supported liek ( a + 3 ) but both of them as the normal terms aint working 

17 . the Join right now just works on the normal 2 join like a join b but ofc the multiple join condition can occur i havent done
    it at all try doing it 

18. In the bytecodes the cursor moving goes as per the btrees but after i kind of thought to go with the b + trees and then like the 
    all of the seek functions kind of are useless as they go with the stack approach and so does the cursors so all of them needs to be 
    rebuild 

19. See in the bytecodes everywhere there is blob conversion or unconversion i need to add the stuff liek dates and the timestamps
   havent quite considered them as a new thing right so eyah 
